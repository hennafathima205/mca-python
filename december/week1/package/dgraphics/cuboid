def cuboid_area(length,breadth,height):
        return 2 *( length * breadth + breadth + height* length)

def cuboid_perimeter(length,breadth,height):
        return 4 * (length + breadth + height)
        
